import React from 'react';
import '../styles.css';

const Hero = () => (
  <section className="hero">
    <h2>Welcome to Community Scheduler</h2>
    <p>Book appointments, manage events, and collaborate with your community.</p>
  </section>
);

export default Hero;
